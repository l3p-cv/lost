import { getElementStyle } from "l3p-core"
import * as math from "shared/math"

import DEFAULTS from "drawables/drawable.defaults"

export default {
    bounds: {
        x: 0,
        y: 0,
    },
    border: {
        colorActiveEdge: "#fff",
    },
    strokeWidth: 4,
}
